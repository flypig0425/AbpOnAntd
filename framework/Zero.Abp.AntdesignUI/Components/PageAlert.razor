@foreach (var alert in Alerts)
{
    @if (alert.IsVisible)
    {
        <Alert Banner ShowIcon="false"
               Type="@GetAlertType(alert.AlertMessage.Type)"
               Message="@alert.AlertMessage.Title"
               Description="@alert.AlertMessage.Text"
               Closable="@alert.AlertMessage.Dismissible" />
    }
}

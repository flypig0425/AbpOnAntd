@typeparam TEntity
@typeparam TResourceType
@using Zero.Abp.AntBlazorUI
@using Volo.Abp.Localization

@if (PropertyInfo != null && Entity != null)
{
    <FormItem>
        <Checkbox @bind-Checked="@Value">@PropertyInfo.GetLocalizedDisplayName(StringLocalizerFactory)</Checkbox>
    </FormItem>
}